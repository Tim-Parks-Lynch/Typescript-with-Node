{
	"name": "typescript-with-node",
	"version": "1.0.0",
	"description": "- If a typescript file has just plain js inside of it and no ts, node will run it as a regular js file. However if it sees typescript specific features like types, it will throw a syntax error. - You must first compile the file using tsc app.ts and then run node app.js in order to run the file.",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon ./dist/app.js"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Tim-Parks-Lynch/Typescript-with-Node.git"
	},
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/Tim-Parks-Lynch/Typescript-with-Node/issues"
	},
	"homepage": "https://github.com/Tim-Parks-Lynch/Typescript-with-Node#readme",
	"dependencies": {
		"body-parser": "^1.19.2",
		"express": "^4.17.3"
	},
	"devDependencies": {
		"@types/express": "^4.17.13",
		"@types/node": "^17.0.21",
		"nodemon": "^2.0.15"
	}
}
